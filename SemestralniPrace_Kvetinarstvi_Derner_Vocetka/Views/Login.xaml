<Window x:Class="SemestralniPrace_Kvetinarstvi_Derner_Vocetka.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Login" HorizontalAlignment="Center" FontSize="18"/>

        <Label Grid.Row="1" Content="Email:"/>
        <TextBox Grid.Row="1" Name="EmailTextBox" Text="{Binding /Email}"/>

        <Label Grid.Row="2" Content="Password:"/>
        <PasswordBox Grid.Row="2" Name="passwordBox" Password="{Binding Password}"/>

        <Button Grid.Row="3" Content="Login" Command="{Binding LoginCommand}"/>
        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="Red"/>
    </Grid>
</Window>