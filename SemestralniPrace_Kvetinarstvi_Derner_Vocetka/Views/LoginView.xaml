<UserControl x:Class="SemestralniPrace_Kvetinarstvi_Derner_Vocetka.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SemestralniPrace_Kvetinarstvi_Derner_Vocetka.Views"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Login" HorizontalAlignment="Center" FontSize="18"/>

        <Label Grid.Row="1" Content="Email:"/>
        <TextBox Grid.Row="1" Name="EmailTextBox" Text="{Binding Email, Mode=TwoWay}"/>

        <Label Grid.Row="2" Content="Password:"/>
        <TextBox Grid.Row="2" Name="PasswordBox" Text="{Binding Password, Mode=TwoWay}"/>

        <Button Grid.Row="3" Content="Login" Command="{Binding LoginCommand}"/>
        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage, Mode=TwoWay}" Foreground="Red"/>
    </Grid>
</UserControl>
